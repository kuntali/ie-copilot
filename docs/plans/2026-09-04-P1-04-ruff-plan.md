# P1-04 — Ruff 全绿计划

**Task:** `P1-04`  
**Method:** Superpowers `systematic-debugging` + `writing-plans` + `verification-before-completion`  
**Status:** execution in progress  
**Live status:** `docs/TASKS.md`

## Root cause evidence

GitHub Actions run #58 (`33853968125`), Python 3.10 job `100962861920`, reaches frozen dependency installation successfully and then Ruff reports exactly 13 errors:

- `src/ie_copilot/agents.py`
  - `UP035`: `Sequence` should come from `collections.abc`
  - `UP037`: unnecessary quoted return annotation
  - `E501`: 4 overlong prompt-string lines
- `src/ie_copilot/cli.py`
  - `I001`: import block ordering
- `src/ie_copilot/observability.py`
  - `UP035`: `Iterator` should come from `collections.abc`
  - `UP037`: unnecessary quoted return annotation
- `src/ie_copilot/protocols.py`
  - `UP035`: `Sequence` should come from `collections.abc`
- `tests/conftest.py`
  - `UP035`: `Sequence` should come from `collections.abc`
  - `E501`: one overlong `Claim(...)` line
- `tests/test_graph.py`
  - `I001`: import block ordering

Ruff reports 8/13 as auto-fixable. No evidence points to a configuration or dependency problem.

## Goal

Make `uv run --python 3.10 ruff check .` and the 3.13 matrix equivalent pass with the smallest formatting/type-import diff, without changing runtime behavior.

## Constraints

- No prompt wording changes except whitespace/string-literal splitting that preserves concatenated content exactly.
- No graph/model behavior changes.
- Do not relax Ruff rules or increase line length to hide violations.
- Keep `from __future__ import annotations`; removing quotes is therefore runtime-safe.
- This is a lint-only task, so RED is the observed Ruff failure from run #58; no new behavioral unit test is required solely for style changes.

## Files and exact changes

1. `src/ie_copilot/agents.py`
   - move `Sequence` import to `collections.abc`;
   - remove quotes around `LLMDebateAgent` return annotation;
   - split overlong adjacent prompt strings without changing resulting text.

2. `src/ie_copilot/cli.py`
   - reorder imports exactly as Ruff requires.

3. `src/ie_copilot/observability.py`
   - import `Iterator` from `collections.abc`, keep `Any` in `typing`;
   - remove quotes around `PhoenixSettings` annotation.

4. `src/ie_copilot/protocols.py`
   - import `Sequence` from `collections.abc`, keep `Protocol` in `typing`.

5. `tests/conftest.py`
   - import `Sequence` from `collections.abc`;
   - wrap the overlong `Claim(...)` construction.

6. `tests/test_graph.py`
   - reorder `conftest`/package imports as Ruff reports.

## Verification

After the minimal edits:

1. trigger CI;
2. require `Ruff` step to pass on Python 3.10;
3. require `Ruff` step to pass on Python 3.13;
4. inspect the next failing step rather than claiming full CI green if pytest fails;
5. run/observe pytest when Ruff no longer blocks it; pytest failures belong to `P1-05` unless caused by the lint-only diff.

## Acceptance criteria

```text
Ruff 3.10 = pass
Ruff 3.13 = pass
Ruff rules unchanged = yes
runtime behavior changed = no
next CI stage becomes visible = yes
```

## Stop conditions

If Ruff after these exact fixes reports a new class of issue not present in run #58, stop and inspect the new output before broad cleanup. Do not run blanket refactors across unrelated files.
